#!/usr/bin/env node

// const commands = {
//   start: 'sd'
// }

// try {
//   commands[cmd]()
// } catch (e) {
//   console.error(e instanceof Error ? `husky - ${e.message}` : e)
//   process.exit(1)
// }
